<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>HealthKitchen 管理者ページ</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <!-- FontAwesome -->
  <script src="https://kit.fontawesome.com/516ecc6ad4.js" crossorigin="anonymous"></script>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
          <span>
            <%= link_to admin_root_path, class: 'navbar-brand font-weight-bold', style: 'color: #806000;' do %>
              <i class="fa-solid fa-heart"></i> ヘルスキッチン
            <% end %>
            <p class="fs-6 text-dark mb-0">健康的な料理を、もっと身近に</p>
          </span>
          <!-- Toggler button for mobile -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <ul class="navbar-nav ml-auto">
            <% if admin_signed_in? %>
              <li class="nav-item">
                <%= link_to admin_root_path, class: 'nav-link', style: 'color: #7F6000;' do %>
                  <i class="fa-solid fa-users me-2"></i> ユーザー一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to admin_genres_path, class: 'nav-link', style: 'color: #7F6000;' do %>
                  <i class="fa-solid fa-tags me-2"></i> ジャンル管理
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to destroy_admin_session_path, method: :delete, class: 'nav-link', style: 'color: #7F6000;' do %>
                  <i class="fa-solid fa-right-from-bracket me-2"></i> ログアウト
                <% end %>
              </li>
            <% end %>
          </ul>
          </div>
        </div> <!-- container -->
      </nav>
    </header>
    
    <!-- フラッシュメッセージ -->
    <% if notice %>
      <div class="alert alert-success alert-dismissible fade show mb-0">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="alert alert-danger alert-dismissible fade show mb-0">
        <%= alert %>
      </div>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
  </body>
</html>